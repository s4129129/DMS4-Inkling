export default function AppHeader() {
  return (
    <header className="hero">
      <p className="hero-kicker">Inkling</p>
      <h1>Inkling Reader Dashboard</h1>
      <p>Upload local PDFs, run a timer, and unlock pages as rewards.</p>
    </header>
  );
}
